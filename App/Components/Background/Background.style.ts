import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  image: {
    position: 'absolute',
    bottom: 0,
    left: 0,
    right: 0,
    resizeMode: 'cover',
    height: 255,
  },
});

export default styles;
