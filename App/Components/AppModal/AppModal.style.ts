import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  modalStyle: {
    margin: 0, // because default modal has margins
    shadowOpacity: 0.16,
    shadowRadius: 8,
    justifyContent: 'flex-end',
  },
});

export default styles;
